git add render.yaml
services:
  - type: static
    name: mdm-front
    buildCommand: "npm install && CI=false npm run build"
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
