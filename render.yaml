services:
  - type: static
    name: mdm-front
    buildCommand: "CI=false npm run build"
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
